#include <stdio.h>
int main()
// perimeter of rectangle
{

    int length, width;
    printf("enter the length of rectangle\n"),
    scanf("%d", &length);

    printf("enter the width of rectangle\n"),
    scanf("%d", &width);

    printf("the perimeter of the rectangle is: %d", 2*(length+width));
   return 0;
}

 #include<stdio.h>
 int main(){
  //  Take a number from user & output its cube;
    int n;
    printf("enter no.n\n");
    scanf("%d", &n);

    printf("cube of n is : %d\n", n*n*n);
 
 return 0;
 }

